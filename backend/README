# Backend con Node, TS y Express

Proyecto con api´s para subir un archivo.json a mongo db. 😎

## Pasos para correr el proyecto

1. Clona este repositorio: `git clone https://github.com/JVIDALES-VF/node-and-flutter-interview.git`
2. Ve al directorio del proyecto: `cd backend`
3. Instala las dependencias: `npm install`
4. Agrega en la carpeta raíz el arcivo .env  

## Uso

Para ejecutar el proyecto en modo desarrollo, simplemente ejecuta: `npm run dev`

## Pasos para la invocación de los Endpoints
En Postman:

Selecciona el método POST.
Ingresa la URL: http://localhost:8000/api/upload.
Ve a la pestaña Body y selecciona form-data.
Agrega un campo con el nombre file y selecciona tu archivo JSON.
Haz clic en Send para enviar la petición. Si todo está configurado correctamente, deberías ver una respuesta indicando que los datos se cargaron con éxito.

## Licencia

Este proyecto está bajo la Licencia MIT. Consulta el archivo LICENSE para más detalles.